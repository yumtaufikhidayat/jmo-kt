<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="16dp">

    <androidx.appcompat.widget.AppCompatTextView
        android:id="@+id/tvProfileMenuName"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        tools:text="@string/txt_profile"
        android:textColor="@color/black"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toStartOf="@id/imgProfileMenu"/>

    <androidx.appcompat.widget.AppCompatImageView
        android:id="@+id/imgProfileMenu"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_baseline_keyboard_arrow_right"
        app:layout_constraintTop_toTopOf="@id/tvProfileMenuName"
        app:layout_constraintEnd_toEndOf="parent"/>

    <View
        android:id="@+id/viewDivider"
        android:layout_width="0dp"
        android:layout_height="2dp"
        android:layout_marginTop="16dp"
        android:background="@android:color/darker_gray"
        app:layout_constraintTop_toBottomOf="@id/tvProfileMenuName"
        app:layout_constraintStart_toStartOf="@id/tvProfileMenuName"
        app:layout_constraintEnd_toEndOf="@id/imgProfileMenu"/>

</androidx.constraintlayout.widget.ConstraintLayout>